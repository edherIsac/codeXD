/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { CodigoIntermedioCodexdService } from './codigoIntermedio-codexd.service';

describe('Service: CodigoIntermedioCodexd', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [CodigoIntermedioCodexdService]
    });
  });

  it('should ...', inject([CodigoIntermedioCodexdService], (service: CodigoIntermedioCodexdService) => {
    expect(service).toBeTruthy();
  }));
});
