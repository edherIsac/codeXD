/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { SintacticoCodexdService } from './sintactico-codexd.service';

describe('Service: SintacticoCodexd', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [SintacticoCodexdService]
    });
  });

  it('should ...', inject([SintacticoCodexdService], (service: SintacticoCodexdService) => {
    expect(service).toBeTruthy();
  }));
});
